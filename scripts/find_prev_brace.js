const fs=require('fs');const path='c:/Users/ash/Documents/GitHub/PiDyn/server/player.html';const s=fs.readFileSync(path,'utf8');const m=s.match(/<script(?![^>]*src)[^>]*>([\s\S]*?)<\/script>/i);const src=m[1];const pos=12598; for(let i=pos;i>0;i--){ if(src[i]==='{'){ const start=Math.max(0,i-120); const end=Math.min(src.length,i+40); console.log('Found { at',i,'line',src.slice(0,i).split('\n').length); console.log(src.slice(start,end)); break;} }
